apiVersion: v1
kind: ConfigMap
metadata:
  name: reducto-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  reducto-dashboard.json: |
{{ .Files.Get "files/reducto-dashboard.json" | indent 4 }}
